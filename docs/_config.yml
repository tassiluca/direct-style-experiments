title: PPS-22-direct-style
description: Documentation for the PPS project
baseurl: "/PPS-22-direct-style-experiments"
source: ./content

# Rendering
markdown: kramdown
kramdown:
  input: GFM                  # uses GH Flavored Markdown
  hard_wrap: false            # do not enter <br> automatically
  syntax_highlighter: rouge   # Uses Rouge for syntax highlighting

# Theme and plugins
remote_theme: pages-themes/primer@v0.6.0
plugins:
  - jekyll-remote-theme
  - jemoji
